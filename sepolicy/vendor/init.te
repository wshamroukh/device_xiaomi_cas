allow init proc_last_kmsg:file {
  r_file_perms
  setattr
};

allow init per_boot_file:file ioctl;
allowxperm init per_boot_file:file ioctl { F2FS_IOC_SET_PIN_FILE };

allow init vendor_sysfs_scsi_host:file w_file_perms;

allow init proc:file { rw_file_perms };
allow init vendor_spunvm_file:filesystem { unmount mount relabelfrom relabelto };

# Allow init to set read_ahead_kb on /data partition
allow init vendor_sysfs_scsi_host:file w_file_perms;

# Allow system_server to control Bypass Charging
allow init sysfs_battery_supply:file rw_file_perms;
